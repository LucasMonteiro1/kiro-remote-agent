{
  "name": "kiro-remote-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Local daemon that bridges a kiro-cli session to a WebSocket hub it hosts itself, so you can keep chatting with your Kiro agent from your phone (via a Cloudflare Tunnel) while it keeps running on your work machine.",
  "bin": {
    "kiro-remote-agent": "dist/index.js"
  },
  "scripts": {
    "postinstall": "node -e \"try{require('fs').chmodSync(require.resolve('node-pty/prebuilds/'+process.platform+'-'+process.arch+'/spawn-helper'),0o755)}catch(e){}\"",
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "db:migrate": "node scripts/migrate.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@xterm/headless": "^6.0.0",
    "dotenv": "^16.4.5",
    "jose": "^5.9.6",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
